# Task 03 Dependency Decisions

Date: 2026-04-11

| Dependency | Change | Evidence pointer(s) | Rationale | Risk / Impact | Owner / Follow-up |
|---|---|---|---|---|---|
| `mineru[vllm]==3.0.7` | Replaced with `mineru==3.0.7` in `requirements.txt` | `requirements.txt:3`; Task 01 matrix flags the `mineru[vllm]`/`vllm` strategy conflict (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:22,32`) | Keep direct MinerU runtime dependency while avoiding extra-level resolver coupling for vLLM that is now base-image managed. | Potential loss of implicit extra dependencies if MinerU packaging changes. Mitigated by runtime checks and Task 04 Docker validation stages. | Keep monitored in dependency checks and Task 04 workflow evidence. |
| `vllm==0.18.0` | Removed from `requirements.txt`; now Docker/base-image managed | Base image pin remains explicit in `Dockerfile` (`Dockerfile:17`); Task 01 matrix tracks `vllm` ownership decision (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:18,38`) | Prevent duplicate dependency ownership (`requirements.txt` + base image) and reduce resolver conflicts while preserving deterministic container runtime via pinned base image tag. | Runtime drift risk if non-Docker local environment expects `requirements.txt` to install `vllm`. Mitigated by explicit runtime verification in dependency checks. | Keep Docker image tag as source of truth for `vllm` ownership. |
| `uvloop==0.21.0` | Removed from `requirements.txt`; now inherited from base image/runtime | Current cleaned set in `requirements.txt:1-8`; base-image package inventory audit (`docker run --rm --entrypoint python3 vllm/vllm-openai:v0.18.0 -c ...`) confirmed preinstall; Task 01 matrix captured it as non-direct/runtime-validatable (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:29`) | Avoid re-pinning infrastructure package already shipped by base image. | Possible version drift across base-image tags. | Re-check on base-image tag upgrades. |
| `psutil==5.9.0` | Removed from `requirements.txt`; now inherited from base image/runtime | `requirements.txt:1-8`; base-image package inventory audit command above; Task 01 matrix row (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:20,30`) | Reduce duplicate ownership and resolver pressure; rely on container baseline. | Handler telemetry path depends on availability. | Keep import validated by runtime dependency checks. |
| `requests>=2.32.3,<3.0.0` | Removed from `requirements.txt`; now inherited from base image/runtime | `requirements.txt:1-8`; base-image package inventory audit command above; Task 01 matrix row (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:31`) | Base image already provides package used by readiness/client utilities. | Runtime compatibility depends on base-image version. | Validate in Docker workflow readiness stages. |
| `httpx==0.28.1` | Removed from `requirements.txt`; now inherited from base image/runtime | `requirements.txt:1-8`; base-image package inventory audit command above; Task 01 matrix row (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:34`) | Remove duplicate management for package already available in migrated image. | Potential API drift if base image updates major versions. | Keep covered by worker/runtime tests. |
| `huggingface_hub==0.36.2` | Removed from `requirements.txt`; now inherited from base image/runtime | `requirements.txt:1-8`; base-image package inventory audit command above; Task 01 matrix row (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:35`) | Prevent redundant pin where base image already carries dependency. | CLI/runtime behavior could differ if base-image version changes. | Confirm during Docker validation and model download stages. |
| `tiktoken==0.12.0` | Removed from `requirements.txt`; now inherited from base image/runtime | `requirements.txt:1-8`; base-image package inventory audit command above; Task 01 matrix row (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:36`) | Avoid duplicate ownership of tokenizer dependency. | Token counting logic depends on stable tokenization behavior. | Keep covered by worker-level tests and Docker e2e checks. |
| `pydantic==2.12.5` | Removed from `requirements.txt`; now inherited from base image/runtime | `requirements.txt:1-8`; base-image package inventory audit command above; Task 01 matrix row (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:39`) | Remove duplicate pin to align with base-image-managed runtime stack. | Settings/model validation behavior depends on available major version. | Verify through settings-related tests and dependency checks. |
| `pydantic-settings==2.12.0` | Removed from `requirements.txt`; now inherited from base image/runtime | `requirements.txt:1-8`; base-image package inventory audit command above; Task 01 matrix row (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:40`) | Same ownership simplification as `pydantic`. | Possible minor-version drift from previous explicit pin. | Keep configuration-loading paths covered by tests. |
| `transformers==4.57.6` | Removed from `requirements.txt`; now inherited from base image/runtime | `requirements.txt:1-8`; base-image package inventory audit command above; Task 01 matrix row (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:42`) | Remove duplicate pin for large dependency already provided by base image. | Tokenizer/config behavior can vary by upstream version. | Validate with Docker-based runtime smoke/e2e. |
| `numpy>=1.25,<2.3` | Removed from `requirements.txt`; now inherited from base image/runtime | `requirements.txt:1-8`; base-image package inventory audit command above; Task 01 matrix row (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:43`) | Avoid constraining a foundational dependency already coupled to base-image stack. | ABI/version mismatch risk for native modules. | Track on base-image updates; keep runtime checks in Docker validation. |
| `openai>=1.99.1,<2.0.0` | Removed from `requirements.txt`; now inherited from base image/runtime | `requirements.txt:1-8`; base-image package inventory audit command above; Task 01 matrix row (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:44`) | Prevent conflict with base image shipping newer OpenAI client while still using OpenAI-compatible runtime path. | API surface differences across major versions. | Validate on worker paths and Docker readiness/e2e workflow. |
| `protobuf>=5.0,<7.0` | Removed from `requirements.txt`; now inherited from base image/runtime | `requirements.txt:1-8`; base-image package inventory audit command above; Task 01 matrix row (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:45`) | Remove redundant constraint from application-managed dependencies. | Transitive dependency compatibility risk. | Observe in dependency/runtime checks. |
| `starlette>=0.30.0,<1.0.0` | Removed from `requirements.txt`; now inherited from base image/runtime | `requirements.txt:1-8`; base-image package inventory audit command above; Task 01 matrix row (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:46`) | Base image already provides web-stack dependency; reduce duplicate management. | Framework version drift can affect API behavior. | Validate via server-readiness and related tests. |
| `filelock>=3.24.2,<4.0.0` | Removed from `requirements.txt`; now inherited from base image/runtime | `requirements.txt:1-8`; base-image package inventory audit command above; Task 01 matrix row (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:47`) | Eliminate redundant explicit transitive pin. | Locking semantics may vary across versions. | Validate around model/cache operations in Docker workflow. |
| `cuda-bindings==12.9.4` | Removed from `requirements.txt`; now inherited from base image/runtime | `requirements.txt:1-8`; base-image package inventory audit command above; Task 01 matrix row (`plans/migrate-to-vllm-docker-image/artifacts/dependency-matrix.md:19,48`) | Let CUDA Python binding ownership follow CUDA-enabled base-image stack. | CUDA/runtime compatibility risk outside container parity. | Validate only in intended Docker/Linux runtime path. |
| `shapely>=2.0.7,<3.0.0` | Removed from `requirements.txt` | `requirements.txt:1-7` shows no `shapely`; `mineru==3.0.7` metadata exposes `shapely` only for extra `pipeline` (`extra == "pipeline"`); current NoteLM path is `vlm-http-client` | Avoid carrying optional geometry dependency not required by supported runtime flow. | If future local pipeline path is enabled, `shapely` may be needed again. | Reintroduce only when runtime path/feature set explicitly requires MinerU pipeline extras. |
| `paddlepaddle-gpu==3.3.0` | Removed from explicit Docker install | Current `Dockerfile` no longer contains Paddle install step (`Dockerfile:78-88`); `check_dependencies.py` contract no longer imports/checks Paddle; in-container `python3 check_dependencies.py` on `notelm` passes | Remove legacy local OCR dependency that is not required for the current `vlm-http-client` architecture. | Legacy/local Paddle-based OCR paths would no longer be preinstalled by default. | Keep removal unless supported runtime contract expands beyond `vlm-http-client`. |
| Docker apt installs (`poppler-utils`, `libglib2.0-0`, `curl`, `zstd`, `gcc`, `g++`, `python3-dev`, `gosu`) | Removed from `Dockerfile` apt install list | Current apt install set reduced to `fonts-noto-core`, `fonts-noto-cjk`, `fontconfig`, `libgl1` (`Dockerfile:80-84`); locally rebuilt `notelm` runtime sanity checks pass (`check_dependencies.py`, `debug_dependencies.py`, `import handler`) | Reduce image complexity and avoid retaining unproven legacy/toolchain packages on top of vLLM base image. | Future features requiring these binaries/toolchains would need explicit reintroduction. | Revalidate on future runtime-path expansion and during Task 04 full GPU integration runs. |

## Notes

- Final cleaned `requirements.txt` intentionally retains only project-managed packages: `mineru`, `runpod`, `langchain-text-splitters`, `json-repair`, `langdetect` (`requirements.txt:3-7`).
- `vllm` remains a strict runtime requirement and is still checked by `check_dependencies.py`; only ownership changed from `requirements.txt` to Docker/base image.
- Startup dependency checks remain runtime-only and are still gated in `handler.py` by `DEBUG=true` via `run_debug_dependency_check_if_enabled()`.
